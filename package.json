{
  "name": "techpulse-ai",
  "version": "1.0.0",
  "private": true,
  "description": "TechPulse AI - Futuristic AI Intelligence & Trend Dashboard",

  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"magenta,cyan\" \"npm run dev:server\" \"npm run dev:client\"",

    "dev:server": "npm run dev --prefix server",

    "dev:client": "npm run dev --prefix client",

    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix server && npm run build --prefix client",

    "start": "npm run start --prefix server",

    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },

  "dependencies": {
    "axios": "^1.19.0",
    "rss-parser": "^3.13.0"
  },

  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}